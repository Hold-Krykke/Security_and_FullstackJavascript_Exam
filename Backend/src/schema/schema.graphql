type Query {
  """
  Multiline docstring
  It is used for providing descriptions of types, fields and arguments.
  Docstrings show up in the documentation panel inside GraphQL playground and GraphiQL.
  Description for the field
  """
  helloWorld: String!
}
type Mutation {
    addUser: Boolean!
}
# You can comment like this here.
# https://github.com/apollographql/graphql-tag#importing-graphql-files
